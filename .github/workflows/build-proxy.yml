name: Build FrenchIPMasker - Proxy Local

on:
  push:
    branches: [main, master]
    paths:
      - 'proxy_version/**'
      - 'proxy_scraper.py'
      - 'proxy_server.py'
      - '.github/workflows/build-proxy.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install customtkinter requests Pillow pyinstaller

      - name: Build executable
        run: |
          pyinstaller --onefile --windowed --name FrenchIPMasker-Proxy `
            --add-data "proxy_scraper.py;." `
            --add-data "proxy_server.py;." `
            --hidden-import customtkinter `
            --hidden-import requests `
            --hidden-import PIL `
            --clean `
            proxy_version/main.py

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: FrenchIPMasker-Proxy
          path: dist/FrenchIPMasker-Proxy.exe

      - name: Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v1.0.0
          files: dist/FrenchIPMasker-Proxy.exe
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
